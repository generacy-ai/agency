# Feature Specification: D4: Port grouping/dependency logic from speckit

**Branch**: `163-d4-port-grouping-dependency` | **Date**: 2026-01-31 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent D** - Ticket & Tasks Tools (`src/tools/get-ticket.ts`, `create-ticket.ts`, `tasks-to-issues.ts`)

## Description
Port the task grouping and dependency resolution logic from speckit for use in tasks-to-issues.

## Acceptance Criteria
- [ ] Create `src/utils/grouping.ts`
- [ ] Create `src/utils/dependency.ts`
- [ ] Support grouping strategies: per-task, per-story, per-phase
- [ ] Implement topological sorting for dependencies
- [ ] Validate circular dependencies
- [ ] Generate auto-dependencies (phase transitions)
- [ ] Convert task groups to issue plans

## Grouping Strategies

| Strategy | Description |
|----------|-------------|
| per-task | One ticket per task (T001 → Issue) |
| per-story | Group tasks by user story (US1 tasks → Issue) |
| per-phase | Group tasks by phase (Phase 1 → Issue) |

## Functions to Implement

```typescript
// src/utils/grouping.ts

export type GroupingStrategy = 'per-task' | 'per-story' | 'per-phase';

export interface IssuePlan {
  title: string;
  body: string;
  labels: string[];
  tasks: Task[];
  dependencies: string[];  // Issue IDs this depends on
}

export function groupTasks(
  tasks: Task[],
  strategy: GroupingStrategy,
): IssuePlan[];

export function generateAutoDependencies(
  tasks: Task[],
  strategy: GroupingStrategy,
): Map<string, string[]>;
```

```typescript
// src/utils/dependency.ts

export interface DependencyValidationResult {
  valid: boolean;
  circularDependencies: string[][];
  missingDependencies: string[];
}

export function validateDependencies(tasks: Task[]): DependencyValidationResult;
export function topologicalSort(tasks: Task[]): Task[];
export function buildDependencyGraph(tasks: Task[]): Map<string, Set<string>>;
```

## Dependencies
- F2 (Task types)
- B5 (task-parser)

## Files to Create/Modify
- `src/utils/grouping.ts`
- `src/utils/dependency.ts`
- `src/utils/index.ts`

## References
- Current grouping: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/utils/grouping.ts`
- Current dependency: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/utils/dependency.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
