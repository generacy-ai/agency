# Feature Specification: C4: Implement update_agent tool

**Branch**: `158-c4-implement-update-agent` | **Date**: 2026-01-31 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent C** - Path & Template Tools (`src/tools/get-paths.ts`, `check-prereqs.ts`, `copy-template.ts`, `update-agent.ts`)

## Description
Implement the `spec_kit.update_agent` MCP tool that updates AI agent context files with technology information from plan.md.

## Acceptance Criteria
- [ ] Create `src/tools/update-agent.ts`
- [ ] Support 17+ agent types (claude, gemini, copilot, cursor-agent, etc.)
- [ ] Extract technology stack from plan.md
- [ ] Update agent-specific context files (CLAUDE.md, .cursorrules, etc.)
- [ ] Maintain Active Technologies and Recent Changes sections
- [ ] Auto-update timestamps
- [ ] Can create agent files from templates if missing

## Tool Definition

```typescript
export function createUpdateAgentTool(config: SpecKitConfig): AgencyTool {
  return {
    name: 'spec_kit.update_agent',
    description: 'Update AI agent context files with technology information from the current feature plan.md',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding'],
    inputSchema: {
      type: 'object',
      properties: {
        agent_type: {
          type: 'string',
          enum: ['claude', 'gemini', 'copilot', 'cursor-agent', 'qwen', 'opencode', 
                 'codex', 'windsurf', 'kilocode', 'auggie', 'roo', 'codebuddy', 
                 'qoder', 'amp', 'shai', 'q', 'bob'],
          description: 'Specific agent to update. If not provided, updates all existing agent files.',
        },
        create_if_missing: {
          type: 'boolean',
          default: false,
          description: 'Whether to create agent file from template if it does not exist',
        },
        feature_dir: {
          type: 'string',
          description: 'Feature directory containing plan.md',
        },
        cwd: {
          type: 'string',
          description: 'Working directory (defaults to process.cwd())',
        },
      },
    },
    async execute(params) {
      // Implementation
    },
  };
}
```

## Agent File Mapping

| Agent Type | File Path |
|------------|-----------|
| claude | CLAUDE.md |
| cursor-agent | .cursorrules |
| copilot | .github/copilot-instructions.md |
| windsurf | .windsurfrules |
| ... | ... |

## Technology Extraction
Parse plan.md for technology mentions in:
- Technology stack sections
- Dependencies
- Framework references
- Library mentions

## Dependencies
- C1 (get_paths)
- C2 (check_prereqs - require plan.md)
- B1 (fs utilities)

## Files to Create/Modify
- `src/tools/update-agent.ts`
- `src/tools/index.ts`

## References
- Current implementation: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/tools/agent.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
