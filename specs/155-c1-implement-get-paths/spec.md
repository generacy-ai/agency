# Feature Specification: C1: Implement get_paths tool

**Branch**: `155-c1-implement-get-paths` | **Date**: 2026-01-30 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent C** - Path & Template Tools (`src/tools/get-paths.ts`, `check-prereqs.ts`, `copy-template.ts`, `update-agent.ts`)

## Description
Implement the `spec_kit.get_paths` MCP tool that resolves all feature-related file paths.

## Acceptance Criteria
- [ ] Create `src/tools/get-paths.ts`
- [ ] Resolve paths based on current branch or explicit feature name
- [ ] Support SPECIFY_FEATURE env var override
- [ ] Respect configuration for custom paths
- [ ] Return complete FeaturePaths object
- [ ] Handle missing feature directory gracefully

## Tool Definition

```typescript
export function createGetPathsTool(config: SpecKitConfig): AgencyTool {
  return {
    name: 'spec_kit.get_paths',
    description: 'Get all feature paths for the current or specified branch',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding', 'research'],
    inputSchema: {
      type: 'object',
      properties: {
        branch: {
          type: 'string',
          description: 'Optional branch/feature name. If not provided, uses current branch or SPECIFY_FEATURE env var.',
        },
        cwd: {
          type: 'string',
          description: 'Working directory (defaults to process.cwd())',
        },
      },
    },
    async execute(params) {
      // Implementation
    },
  };
}
```

## Return Structure

```typescript
interface FeaturePaths {
  repoRoot: string;           // /path/to/repo
  featureDir: string;         // /path/to/repo/specs/001-feature-name
  specFile: string;           // /path/to/repo/specs/001-feature-name/spec.md
  planFile: string;           // /path/to/repo/specs/001-feature-name/plan.md
  tasksFile: string;          // /path/to/repo/specs/001-feature-name/tasks.md
  clarificationsFile: string; // /path/to/repo/specs/001-feature-name/clarifications.md
  contractsDir: string;       // /path/to/repo/specs/001-feature-name/contracts
  checklistsDir: string;      // /path/to/repo/specs/001-feature-name/checklists
  branch: string;             // 001-feature-name
}
```

## Path Resolution Logic
1. Check SPECIFY_FEATURE env var
2. If not set, get current git branch
3. Extract feature number/name from branch
4. Construct paths using config.paths.specs directory
5. Apply any custom file name overrides from config

## Dependencies
- F2 (FeaturePaths type)
- F4 (config schema)
- B1 (fs utilities)
- B2 (git utilities for branch detection)

## Files to Create/Modify
- `src/tools/get-paths.ts`
- `src/tools/index.ts`

## References
- Current implementation: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/tools/paths.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
