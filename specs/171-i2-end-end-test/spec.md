# Feature Specification: I2: End-to-end test: GitHub provider flow

**Branch**: `171-i2-end-end-test` | **Date**: 2026-02-01 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Description
Create end-to-end tests for the complete spec-kit workflow using the GitHub provider.

## Acceptance Criteria
- [ ] Test get_ticket with GitHub URLs and #N references
- [ ] Test create_feature from GitHub issue
- [ ] Test create_ticket creates GitHub issue
- [ ] Test tasks_to_issues creates issues from tasks.md
- [ ] Test full workflow: specify → clarify → plan → tasks → issues
- [ ] Document any GitHub-specific behaviors

## Test Scenarios

### Scenario 1: Basic Ticket Fetch
```typescript
test('get_ticket fetches GitHub issue by number', async () => {
  const result = await callTool('spec_kit.get_ticket', { ref: '#1' });
  expect(result.title).toBeDefined();
  expect(result.url).toMatch(/github\.com/);
});
```

### Scenario 2: Create Feature from Issue
```typescript
test('create_feature from GitHub issue', async () => {
  const ticket = await callTool('spec_kit.get_ticket', { ref: '#1' });
  const feature = await callTool('spec_kit.create_feature', {
    description: ticket.title,
  });
  expect(feature.branch).toMatch(/^\d{3}-/);
  expect(feature.featureDir).toContain('specs/');
});
```

### Scenario 3: Tasks to Issues
```typescript
test('tasks_to_issues creates GitHub issues', async () => {
  const result = await callTool('spec_kit.tasks_to_issues', {
    dry_run: false,
    grouping: 'per-task',
  });
  expect(result.issues.every(i => i.status === 'created')).toBe(true);
});
```

## Test Setup Requirements
- GitHub CLI (`gh`) authenticated
- **Dedicated test repository**: `generacy-ai/speckit-test-fixtures` (or similar) to isolate E2E tests from production issue tracker
- **Dynamic fixtures**: Tests create their own test issues/branches before each run (following `local-flow.test.ts` pattern)
- **Cleanup with preservation flag**: Resources cleaned up by default; set `PRESERVE_TEST_RESOURCES=true` to keep for debugging

## Dependencies
- I1 (plugin wired up)
- A2 (GitHubProvider)

## Files to Create
- `src/__tests__/integration/github-flow.test.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- GitHub CLI (`gh`) is authenticated with appropriate permissions for the test repository
- A dedicated test repository exists with issues enabled
- Test runs in CI have network access to GitHub API
- Tool names are stable: `spec_kit.get_ticket`, `spec_kit.create_feature`, `spec_kit.tasks_to_issues` (verified in `packages/agency-plugin-spec-kit/src/manifest.ts`)

## Out of Scope

- Mock-based testing (covered by existing unit tests in `github-provider.test.ts` and `github-cli.test.ts`)
- Testing against production repositories
- Performance/load testing of GitHub API

---

*Generated by speckit*
