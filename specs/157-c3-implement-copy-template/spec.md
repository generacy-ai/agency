# Feature Specification: C3: Implement copy_template tool

**Branch**: `157-c3-implement-copy-template` | **Date**: 2026-01-31 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent C** - Path & Template Tools (`src/tools/get-paths.ts`, `check-prereqs.ts`, `copy-template.ts`, `update-agent.ts`)

## Description
Implement the `spec_kit.copy_template` MCP tool that instantiates template files into feature directories.

## Acceptance Criteria
- [ ] Create `src/tools/copy-template.ts`
- [ ] Support templates: spec, plan, tasks, checklist, agent-file
- [ ] Allow copying multiple templates in one call
- [ ] Support custom destination filenames
- [ ] Place checklists in checklists/ subdirectory
- [ ] Create directories as needed
- [ ] Respect configured template paths

## Tool Definition

```typescript
export function createCopyTemplateTool(config: SpecKitConfig): AgencyTool {
  return {
    name: 'spec_kit.copy_template',
    description: 'Copy one or more templates to the feature directory',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding'],
    inputSchema: {
      type: 'object',
      properties: {
        templates: {
          type: 'array',
          items: {
            type: 'string',
            enum: ['spec', 'plan', 'tasks', 'checklist', 'agent-file'],
          },
          minItems: 1,
          description: 'List of template names to copy',
        },
        dest_filename: {
          type: 'string',
          description: 'Optional custom destination filename (only valid when copying single template)',
        },
        feature_dir: {
          type: 'string',
          description: 'Target feature directory. If not provided, must specify cwd.',
        },
        cwd: {
          type: 'string',
          description: 'Working directory (defaults to process.cwd())',
        },
      },
      required: ['templates'],
    },
    async execute(params) {
      // Implementation
    },
  };
}
```

## Template Mapping

| Template | Source File | Default Destination |
|----------|-------------|---------------------|
| spec | spec.md | {feature_dir}/spec.md |
| plan | plan.md | {feature_dir}/plan.md |
| tasks | tasks.md | {feature_dir}/tasks.md |
| checklist | checklist.md | {feature_dir}/checklists/{name}.md |
| agent-file | CLAUDE.md | {repo_root}/CLAUDE.md |

## Dependencies
- C1 (get_paths for path resolution)
- B1 (fs utilities)
- C5 (template definitions)
- F4 (config for template paths)

## Files to Create/Modify
- `src/tools/copy-template.ts`
- `src/tools/index.ts`

## References
- Current implementation: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/tools/templates.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
