# Feature Specification: D2: Implement create_ticket tool

**Branch**: `161-d2-implement-create-ticket` | **Date**: 2026-01-30 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent D** - Ticket & Tasks Tools (`src/tools/get-ticket.ts`, `create-ticket.ts`, `tasks-to-issues.ts`)

## Description
Implement the `spec_kit.create_ticket` MCP tool that creates tickets in the configured backlog provider.

## Acceptance Criteria
- [ ] Create `src/tools/create-ticket.ts`
- [ ] Support title, body, and labels
- [ ] Use configured BacklogProvider
- [ ] Return created Ticket with URL
- [ ] Handle creation errors gracefully
- [ ] Support all provider types

## Tool Definition

```typescript
export function createCreateTicketTool(
  config: SpecKitConfig,
  getProvider: () => BacklogProvider,
): AgencyTool {
  return {
    name: 'spec_kit.create_ticket',
    description: 'Create a new ticket in the configured backlog system',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding'],
    inputSchema: {
      type: 'object',
      properties: {
        title: {
          type: 'string',
          description: 'Ticket title',
        },
        body: {
          type: 'string',
          description: 'Ticket body/description (markdown supported)',
        },
        labels: {
          type: 'array',
          items: { type: 'string' },
          description: 'Labels to add to the ticket',
        },
      },
      required: ['title'],
    },
    async execute(params) {
      const { title, body, labels } = params as TicketCreateParams;
      
      const provider = getProvider();
      const ticket = await provider.createTicket({ title, body, labels });
      
      return {
        content: [{
          type: 'text',
          text: JSON.stringify({
            created: true,
            id: ticket.ref.id,
            url: ticket.url,
          }),
        }],
      };
    },
  };
}
```

## Dependencies
- A1 (provider registry)
- F2 (Ticket types)
- F3 (BacklogProvider interface)
- D1 (shared patterns)

## Files to Create/Modify
- `src/tools/create-ticket.ts`
- `src/tools/index.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
