# Tasks: Configuration Schema & File Management

**Input**: Design documents from `/specs/095-tg-005-44-us1/`
**Prerequisites**: plan.md (required), spec.md (required), data-model.md (available)
**Status**: Complete

## Format: `[ID] [P?] [Story] Description`
- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to

## Phase 1: Setup

- [X] T001 Create config directory structure at `packages/agency-extension/src/config/`
- [X] T002 Verify zod dependency in `packages/agency-extension/package.json` (should already exist from parent)

## Phase 2: Schema Implementation

- [X] T003 [P] [US1] Implement `ConfigSchema.ts` with Zod schemas for `PluginConfig`, `ModeConfig`, `ContainerConfig`, `AgencyConfig`
- [X] T004 [P] [US1] Implement `defaults.ts` with `DEFAULT_CONFIG` constant and version
- [X] T005 [US1] Create `index.ts` exporting all schemas, types, and defaults

## Phase 3: File Operations

- [X] T006 [US1] Implement `ConfigFile.ts` class with constructor and `getPath()` method
- [X] T007 [US1] Implement `ConfigFile.read()` method with schema validation and default fallback
- [X] T008 [US1] Implement `ConfigFile.write()` method with atomic writes and directory creation
- [X] T009 [US1] Implement `ConfigFile.exists()` method using VS Code workspace API
- [X] T010 [US1] Implement `ConfigFile.watch()` method using `vscode.workspace.createFileSystemWatcher()`

## Phase 4: Testing

- [X] T011 [US1] Create test directory at `packages/agency-extension/src/__tests__/config/`
- [X] T012 [US1] Write tests for schema validation (valid/invalid inputs) in `ConfigSchema.test.ts`
- [X] T013 [US1] Write tests for `ConfigFile.read()` (missing file, invalid JSON, valid config) in `ConfigFile.test.ts`
- [X] T014 [US1] Write tests for `ConfigFile.write()` (directory creation, atomic writes) in `ConfigFile.test.ts`
- [X] T015 [US1] Write tests for `ConfigFile.watch()` (external file changes) in `ConfigFile.test.ts`
- [X] T016 [US1] Run all tests with `pnpm test` and verify >90% coverage

## Dependencies & Execution Order

### Sequential Dependencies:
1. **Phase 1 → Phase 2**: Directory structure must exist before creating files
2. **T003 → T005**: Schemas must be defined before exporting
3. **T004 → T005**: Defaults must be defined before exporting
4. **T003,T004 → T006**: Schemas and defaults must exist before ConfigFile implementation
5. **T006 → T007-T010**: ConfigFile class structure must exist before implementing methods
6. **Phase 3 → Phase 4**: Implementation must be complete before testing

### Parallel Opportunities:
- **T003 and T004**: Can be implemented in parallel (different files, no dependencies)
- **T007, T008, T009, T010**: Can be implemented in parallel (independent methods)
- **T012, T013, T014, T015**: Can be written in parallel (independent test files)

### Priority Markers:
- All tasks are P1 (foundational infrastructure for the epic)

---

*Generated by speckit*
