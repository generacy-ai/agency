# Feature Specification: A2: GitHubProvider implementation

**Branch**: `146-a2-githubprovider-implementation` | **Date**: 2026-01-30 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent A** - Backlog Providers (`src/providers/*`)

## Description
Implement the GitHubProvider that uses the `gh` CLI for GitHub issue operations. This ports and adapts the existing GitHub integration from speckit.

## Acceptance Criteria
- [ ] Create `src/providers/github.ts`
- [ ] Implement all BacklogProvider interface methods
- [ ] Use `gh` CLI for all GitHub operations
- [ ] Support authentication check via `gh auth status`
- [ ] Parse GitHub URLs and issue numbers
- [ ] Handle rate limiting and errors gracefully
- [ ] Support label operations

## Implementation Details

```typescript
export class GitHubProvider implements BacklogProvider {
  readonly name = 'github' as const;
  
  async getTicket(ref: string): Promise<Ticket> {
    const parsed = this.parseRef(ref);
    // gh issue view <number> --json title,body,state,labels,url
  }
  
  async createTicket(params: TicketCreateParams): Promise<Ticket> {
    // gh issue create --title "..." --body "..." --label "..."
  }
  
  async updateTicket(ref: string, updates: TicketUpdates): Promise<Ticket> {
    // gh issue edit <number> --title "..." --body "..."
  }
  
  async setLabels(ref: string, labels: string[]): Promise<void> {
    // gh issue edit <number> --add-label "..." --remove-label "..."
  }
  
  async checkAuth(): Promise<{ ok: boolean; message?: string }> {
    // gh auth status
  }
  
  parseRef(input: string): TicketRef | null {
    // Parse: #123, 123, https://github.com/owner/repo/issues/123
  }
  
  getTicketUrl(ref: string): string {
    // Generate full GitHub URL
  }
}
```

## Dependencies
- A1 (provider registry)
- F3 (BacklogProvider interface)

## Files to Create/Modify
- `src/providers/github.ts`

## References
- Current implementation: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/utils/github.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
