# Feature Specification: B4: Implement create_feature tool

**Branch**: `153-b4-implement-create-feature` | **Date**: 2026-01-31 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent B** - Git & Feature Tools (`src/utils/*`, `src/tools/git-ops.ts`, `src/tools/create-feature.ts`)

## Description
Implement the `spec_kit.create_feature` MCP tool that initializes a new feature with branch and spec directory.

## Acceptance Criteria
- [ ] Create `src/tools/create-feature.ts`
- [ ] Generate branch name from description (slug generation)
- [ ] Auto-number features (find next available number)
- [ ] Create feature directory structure
- [ ] Initialize spec.md from template
- [ ] Support parent epic branches
- [ ] Respect configuration for paths and naming

## Tool Definition

```typescript
export function createCreateFeatureTool(config: SpecKitConfig): AgencyTool {
  return {
    name: 'spec_kit.create_feature',
    description: 'Create a new feature branch and initialize the spec directory with template files',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding'],
    inputSchema: {
      type: 'object',
      properties: {
        description: {
          type: 'string',
          description: 'Feature description used to generate spec content',
        },
        short_name: {
          type: 'string',
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$',
          description: 'Optional 2-4 word short name for the branch',
        },
        number: {
          type: 'integer',
          minimum: 1,
          maximum: 999,
          description: 'Optional explicit branch number',
        },
        parent_epic_branch: {
          type: 'string',
          description: 'Parent epic branch to branch from (for epic children)',
        },
        cwd: {
          type: 'string',
          description: 'Working directory (defaults to process.cwd())',
        },
      },
      required: ['description'],
    },
    async execute(params) {
      // Implementation
    },
  };
}
```

## Slug Generation Logic
- Convert description to lowercase
- Remove stop words (the, a, an, and, or, etc.)
- Take first N words (configurable, default 4)
- Join with hyphens
- Sanitize special characters

## Directory Structure Created
```
specs/{number}-{slug}/
├── spec.md (from template)
├── checklists/
└── contracts/
```

## Dependencies
- B1 (fs utilities)
- B2 (git utilities)
- B3 (git_ops tool)
- C1 (get_paths - for path resolution)

## Files to Create/Modify
- `src/tools/create-feature.ts`
- `src/utils/slug.ts` (slug generation helper)

## References
- Current implementation: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/tools/feature.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
