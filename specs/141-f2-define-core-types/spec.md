# Feature Specification: F2: Define core types (Feature, Paths, TicketRef, etc.)

**Branch**: `141-f2-define-core-types` | **Date**: 2026-01-30 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Description
Define the TypeScript types that will be shared across all spec-kit tools.

## Acceptance Criteria
- [ ] Create `src/types/feature.ts` - Feature, FeaturePaths, BranchInfo
- [ ] Create `src/types/ticket.ts` - Ticket, TicketRef, TicketParams, TicketUpdates
- [ ] Create `src/types/task.ts` - Task, TaskGroup, TaskDependency, GroupingStrategy
- [ ] Create `src/types/clarification.ts` - ClarificationQuestion, ClarificationBatch
- [ ] Create `src/types/config.ts` - SpecKitConfig schema with Zod
- [ ] Create `src/types/index.ts` - Re-export all types
- [ ] All types should be well-documented with JSDoc comments

## Key Types to Port/Adapt from speckit

```typescript
// Feature paths
interface FeaturePaths {
  repoRoot: string;
  featureDir: string;
  specFile: string;
  planFile: string;
  tasksFile: string;
  clarificationsFile: string;
  contractsDir: string;
  checklistsDir: string;
  branch: string;
}

// Ticket reference (provider-agnostic)
interface TicketRef {
  provider: 'github' | 'jira' | 'shortcut' | 'local';
  id: string;           // "123" or "PROJ-123"
  url?: string;         // Full URL if available
  raw: string;          // Original input
}
```

## Dependencies
- F1 (package structure)

## References
- Current speckit types: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/types/`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
