# Tasks: ConfigService Implementation

**Input**: Design documents from `/specs/045-tg-006-us1-configservice/`
**Prerequisites**: plan.md (required), spec.md (required)
**Status**: Complete

## Format: `[ID] [P?] Description`
- **[P]**: Task can run in parallel with other `[P]` tasks

---

## Phase 1: Setup

### T001 Create services directory structure
**Files**:
- `packages/agency-extension/src/services/index.ts`

- [X] Create `src/services/` directory if it doesn't exist
- [X] Create `index.ts` with exports for services

---

## Phase 2: Core Implementation

### T002 Implement ConfigService singleton class
**Files**:
- `packages/agency-extension/src/services/ConfigService.ts`

- [X] Create ConfigService class with private constructor
- [X] Implement static `getInstance()` method
- [X] Add `initialize()` method for async setup
- [X] Implement `isInitialized()` check
- [X] Add `dispose()` for cleanup
- [X] Add static `reset()` method for testing

---

### T003 Add configuration getter methods
**Files**:
- `packages/agency-extension/src/services/ConfigService.ts`

- [X] Implement `getConfig()` returning full config
- [X] Implement `getPlugins()` returning plugin array
- [X] Implement `getModes()` returning mode array
- [X] Implement `getContainers()` returning container array
- [X] Implement `getPlugin(id)` for single plugin lookup
- [X] Implement `getMode(id)` for single mode lookup
- [X] Implement `getContainer(id)` for single container lookup

---

### T004 Add configuration save methods
**Files**:
- `packages/agency-extension/src/services/ConfigService.ts`

- [X] Implement `savePluginConfig(plugin)` to add/update plugin
- [X] Implement `saveModeConfig(mode)` to add/update mode
- [X] Implement `saveContainerConfig(container)` to add/update container
- [X] Implement `removePlugin(id)` to delete plugin
- [X] Implement `removeMode(id)` to delete mode
- [X] Implement `removeContainer(id)` to delete container
- [X] Validate config before saving using schemas

---

### T005 Implement onConfigChange event emitter
**Files**:
- `packages/agency-extension/src/services/ConfigService.ts`

- [X] Create EventEmitter for config changes
- [X] Expose `onConfigChange` event as public property
- [X] Fire event when config is loaded/reloaded
- [X] Fire event when config is saved
- [X] Wire up file watcher to fire events on external changes

---

### T006 Implement config migration support
**Files**:
- `packages/agency-extension/src/services/ConfigService.ts`

- [X] Define migration interface for version changes
- [X] Implement `migrateConfig()` function
- [X] Apply migrations during `initialize()` if version mismatch
- [X] Log migration operations

---

## Phase 3: Testing

### T007 Write comprehensive unit tests
**Files**:
- `packages/agency-extension/src/__tests__/services/ConfigService.test.ts`

- [X] Test singleton pattern (getInstance returns same instance)
- [X] Test initialization with mock VS Code module
- [X] Test getter methods return expected data
- [X] Test save methods update config correctly
- [X] Test event emitter fires on changes
- [X] Test config migration scenarios
- [X] Test error handling for invalid configs
- [X] Test dispose cleans up resources

---

## Dependencies

- T001 must complete before T002-T006
- T002 must complete before T003-T006
- T003, T004, T005, T006 can run in parallel after T002
- T007 requires T002-T006 to be complete

---

*Generated by speckit*
