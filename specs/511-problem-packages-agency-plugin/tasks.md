# Tasks: Redirect /plan step 5 from CLAUDE.md to specs/<feature>/stack.md

**Input**: Design documents from `/specs/511-problem-packages-agency-plugin/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, contracts/plan-step5.md
**Status**: Complete

## Format: `[ID] [P?] [Story] Description`
- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to

## Phase 1: Setup

- [ ] T001 Confirm baseline: `diff packages/agency-plugin-spec-kit/commands/plan.md packages/claude-plugin-agency-spec-kit/commands/plan.md` is empty (copies start byte-identical) and locate the current step 5 (`update_agent` block) in both files.

## Phase 2: Core Implementation

- [ ] T002 [US1] Rewrite step 5 in `packages/agency-plugin-spec-kit/commands/plan.md` using the exact replacement text from `contracts/plan-step5.md`: instruct writing `specs/<feature>/stack.md`, exact header line `Generated by /plan — do not hand-edit.` (em dash U+2014), record language/framework/dependencies/notable decisions, full-overwrite-never-append semantics (FR-008), and the single prohibition sentence naming `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `.github/copilot-instructions.md` (FR-001, FR-003). Remove the `update_agent` call. Leave the `constitution.md` sync step untouched (FR-007).
- [ ] T003 [US1] Apply the byte-identical rewrite to `packages/claude-plugin-agency-spec-kit/commands/plan.md`, then re-run `diff` to confirm the two copies remain byte-identical (FR-002). Depends on T002.

## Phase 3: Tests

- [ ] T004 [US2] Create pin test `packages/agency-plugin-spec-kit/tests/plan-command-pin.test.ts` (fs + `fileURLToPath(import.meta.url)`, reading the mirror via `../../claude-plugin-agency-spec-kit/commands/plan.md`, per D5). Assert, for **both** copies: presence of P1–P4 from `contracts/plan-step5.md` (`specs/<feature>/stack.md`, the exact header line, overwrite-not-append, the prohibition sentence); `/update[_-]?agent/i` has zero matches anywhere (B1); every match of `/claude\.md/i`, `/agents\.md/i`, `/gemini\.md/i`, `/copilot-instructions\.md/i` (B2) and `/agent[\s-]context/i` (B3) falls inside the prohibition sentence's `[start,end)` range; and raw bytes of the two copies are equal. Depends on T002, T003 (FR-004, SC-002). Do NOT weaken or delete an assertion to make the test pass — the pin is a drift audit; weakening it deletes its value.

## Phase 4: Docs & Changeset

- [ ] T005 [P] [US1] Update `packages/claude-plugin-agency-spec-kit/README.md` lines describing old step 5 — line ~104 ("Updates agent context files (CLAUDE.md, etc.)") and the `update_agent`/`/speckit:plan` row in the tool table (~line 285) — to reflect the new `stack.md` behavior. Leave the `update_agent` tool row in `packages/agency-plugin-spec-kit/README.md:51` intact (tool unchanged, FR-007) (FR-005).
- [ ] T006 [P] [US1] Add a changeset in `.changeset/` naming only `@generacy-ai/agency-plugin-spec-kit` with a **minor** bump (Q4/FR-006); the body notes the deprecated mirror `claude-plugin-agency-spec-kit` copy was updated in lockstep (it has no `package.json`, so it cannot be a changeset target — see plan deviation note).

## Phase 5: Verification

- [ ] T007 [US2] Run `pnpm --filter @generacy-ai/agency-plugin-spec-kit test` — the new pin test, `tests/tools/update-agent.test.ts` (must still pass, FR-007/SC-003), and `tests/commands.test.ts` all pass.
- [ ] T008 [US1] SC-001 grep checks: `grep -ri update_agent packages/agency-plugin-spec-kit/commands/plan.md packages/claude-plugin-agency-spec-kit/commands/plan.md` returns nothing; `grep -i "claude.md" packages/*/commands/plan.md` matches only the single prohibition sentence.
- [ ] T009 [US2] Negative check (SC-002): temporarily revert step 5 in one copy, confirm the pin test fails, then restore. Confirm `.changeset/` contains the minor-bump changeset (SC-004).

## Dependencies & Execution Order

- **T001** (baseline) before everything.
- **T002 → T003**: the mirror rewrite must copy the canonical rewrite exactly; do them in sequence to preserve byte-identity.
- **T004** depends on T002 + T003 (it pins the final text of both copies).
- **T005** and **T006** are `[P]` — different files (README vs `.changeset/`), independent of the pin test; can run in parallel after T003.
- **Phase 5 (T007–T009)** runs last: T007 needs the test file (T004); T008 needs the rewritten commands (T003); T009 needs both the test (T004) and the changeset (T006).

**Parallel opportunities**: T005 and T006 can run concurrently. All other tasks are sequential due to the byte-identity invariant and test-after-edit ordering.
