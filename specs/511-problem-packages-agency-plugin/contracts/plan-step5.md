# Contract: /plan step 5 replacement text

This is the exact text that replaces step 5 in both `plan.md` copies
(`packages/agency-plugin-spec-kit/commands/plan.md` and
`packages/claude-plugin-agency-spec-kit/commands/plan.md`). The pin test asserts the
quoted strings below verbatim; any wording change requires re-pinning the test in the
same PR.

## Replacement step 5

```markdown
5. **Write the tech stack summary** to `specs/<feature>/stack.md`:
   - The file MUST begin with this exact first line: `Generated by /plan — do not hand-edit.`
   - Then record the feature's technical context: language, framework, dependencies, and notable technical decisions
   - Fully overwrite `stack.md` on every `/plan` run — never append; prior runs are recoverable from git history
   - The plan phase must NEVER modify repo-root agent-context files (`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `.github/copilot-instructions.md`)
```

Note: the old step 5 called the `update_agent` MCP tool. The new step 5 makes no MCP
tool call, so the "Maximum 3 MCP tool calls" constraint in the command still holds
(`get_paths` + `check_prereqs` leaves headroom).

## Pinned strings (must appear in both copies)

| ID | Pinned string / pattern | Kind |
|----|-------------------------|------|
| P1 | `specs/<feature>/stack.md` | presence |
| P2 | `Generated by /plan — do not hand-edit.` (exact, em dash U+2014) | presence |
| P3 | `Fully overwrite` … `never append` (overwrite semantics, Q2) | presence |
| P4 | `must NEVER modify repo-root agent-context files (`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `.github/copilot-instructions.md`)` | presence — this is the single prohibition sentence |

## Banned strings (scoped per Q1)

| ID | Pattern | Scope |
|----|---------|-------|
| B1 | `/update[_-]?agent/i` | banned everywhere in both copies |
| B2 | `/claude\.md/i`, `/agents\.md/i`, `/gemini\.md/i`, `/copilot-instructions\.md/i` | allowed only within the P4 prohibition sentence |
| B3 | `/agent[\s-]context/i` | allowed only within the P4 prohibition sentence |

## Identity

The raw bytes of the two `plan.md` files must be equal.
