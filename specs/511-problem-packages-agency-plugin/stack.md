Generated by /plan — do not hand-edit.

- **Language**: TypeScript (ESM) for the pin test; Markdown for command playbooks
- **Framework**: Vitest (`pnpm --filter @generacy-ai/agency-plugin-spec-kit test`)
- **Dependencies**: none added — uses `node:fs`, `node:path`, `node:url` only
- **Notable decisions**:
  - New step 5 writes `specs/<feature>/stack.md` (overwrite on re-run); prohibition sentence names forbidden agent-context files literally (Q1/Q2)
  - Pin test in `packages/agency-plugin-spec-kit/tests/plan-command-pin.test.ts` asserts presence + scoped absence + byte-identity of both `plan.md` copies (Q3)
  - Changeset: minor bump for `@generacy-ai/agency-plugin-spec-kit` only — the `claude-plugin-agency-spec-kit` mirror has no `package.json` and is not versionable (Q4)
  - `update_agent` tool, its tests, and `constitution.md` sync step untouched
