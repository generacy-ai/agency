# Feature Specification: Redirect /plan step 5 from CLAUDE.md to specs/<feature>/stack.md

**Branch**: `511-problem-packages-agency-plugin` | **Date**: 2026-08-28 | **Status**: Draft
**Issue**: [generacy-ai/agency#511](https://github.com/generacy-ai/agency/issues/511)

## Summary

The `/plan` slash command playbook — shipped in two byte-identical copies at
`packages/agency-plugin-spec-kit/commands/plan.md` and
`packages/claude-plugin-agency-spec-kit/commands/plan.md` — instructs the agent in
step 5 to call the `update_agent` MCP tool and write new technology info into the
repo-root `CLAUDE.md`. Because Generacy cluster workers copy these `commands/*.md`
files verbatim into `~/.claude/commands/` and run the bare `/plan` command, every
feature branch's plan phase writes per-feature, transient content into `CLAUDE.md`.

This bloats `CLAUDE.md` (observed ~285 KB / ~71K tokens in one run), causes repeated
merge conflicts between sibling branches, and — when `update_agent` is unavailable —
prompts the model to hand-write freeform planning narratives into `CLAUDE.md`, which
is worse than the tool's bounded output.

The fix rewrites step 5 so the plan phase writes per-feature technical context to
`specs/<feature>/stack.md` and never touches repo-root agent-context files, then adds
a pin test guarding against regression. The `update_agent` tool itself and the
`constitution.md` sync step are left untouched (those write durable conventions).

## User Stories

### US1: Plan phase writes tech context to the feature directory (Primary)

**As a** Generacy cluster worker (or a developer running `/plan` locally),
**I want** the plan phase to record per-feature technical context in
`specs/<feature>/stack.md`,
**So that** transient, feature-scoped planning notes stay isolated to the feature
branch instead of accumulating in and conflicting over the repo-root `CLAUDE.md`.

**Acceptance Criteria**:
- [ ] Step 5 of both `plan.md` copies instructs writing `specs/<feature>/stack.md`.
- [ ] `stack.md` begins with the header line `Generated by /plan — do not hand-edit.`
      followed by language / framework / dependencies / notable decisions.
- [ ] On re-runs of `/plan` for the same feature, `stack.md` is fully overwritten —
      it always reflects the latest plan run; history lives in git (Q2).
- [ ] Step 5 explicitly states the plan phase must NEVER modify repo-root
      agent-context files (`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`,
      `.github/copilot-instructions.md`) — the forbidden filenames are named
      literally in this single prohibition sentence (Q1).

### US2: Regression is caught automatically

**As a** maintainer of the spec-kit packages,
**I want** a pin test that fails if step 5's old behavior is reintroduced or the two
`plan.md` copies drift apart,
**So that** the fix from generacy#899 cannot silently regress.

**Acceptance Criteria**:
- [ ] A pin test asserts neither `plan.md` copy mentions `update_agent` anywhere;
      `CLAUDE.md` (and the other agent-context filenames) and the phrase
      "agent context" are permitted only within the single NEVER-modify
      prohibition sentence (Q1).
- [ ] The pin test positively asserts the new contract in both copies: the
      `specs/<feature>/stack.md` instruction, the exact header line
      `Generated by /plan — do not hand-edit.`, and the prohibition
      statement (Q3).
- [ ] The pin test asserts the two `plan.md` copies remain byte-identical.
- [ ] The pin test fails if the old step 5 is restored or the stack.md
      instruction is dropped.

### US3: The update_agent tool remains available and correct

**As a** consumer of the `constitution.md` sync workflow,
**I want** the `update_agent` MCP tool and its existing tests left untouched,
**So that** durable convention sync (a legitimate use) continues to work.

**Acceptance Criteria**:
- [ ] The `update_agent` tool implementation is unchanged.
- [ ] `packages/agency-plugin-spec-kit/tests/tools/update-agent.test.ts` still passes.

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | Rewrite step 5 in `packages/agency-plugin-spec-kit/commands/plan.md` to write `specs/<feature>/stack.md` | P1 | Header line + language/framework/dependencies/notable decisions |
| FR-002 | Apply the identical rewrite to `packages/claude-plugin-agency-spec-kit/commands/plan.md` | P1 | Must remain byte-identical to FR-001 copy |
| FR-003 | Step 5 text must explicitly forbid touching `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `.github/copilot-instructions.md` | P1 | Model hand-edits when unsure — be explicit |
| FR-004 | Add a pin test in `packages/agency-plugin-spec-kit/tests/` asserting: no `update_agent` mention anywhere in either copy; `CLAUDE.md`/"agent context" appear only inside the single NEVER-modify sentence; both copies instruct writing `specs/<feature>/stack.md` with the exact header line and contain the prohibition; both copies byte-identical | P1 | Q1 + Q3: absence checks scoped around the prohibition sentence, plus positive contract assertions |
| FR-005 | Update any README/docs in the spec-kit packages that describe old step 5 | P2 | |
| FR-006 | Include a changeset for both spec-kit packages with a **minor** bump | P1 | Q4: user-visible behavior change (new stack.md artifact), not a pure fix |
| FR-008 | Step 5 must specify that `stack.md` is fully overwritten on each `/plan` run (no appending) | P1 | Q2: appending would recreate the accumulating-log pathology in a new file |
| FR-007 | Leave `update_agent` tool and `constitution.md` sync step unchanged | P1 | Constitution sync writes durable conventions |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | No stray CLAUDE.md/update_agent references in plan commands | `update_agent`: 0 matches; `claude.md`: matches only in the NEVER-modify sentence | `grep -i "update_agent" packages/*/commands/plan.md` returns nothing; `grep -i "claude.md" packages/*/commands/plan.md` matches only the single prohibition sentence (Q1) |
| SC-002 | New pin test passes and guards regression | Pass; fails on reintroduction | Run test suite; revert step 5 and confirm failure |
| SC-003 | Existing update-agent test still passes | Pass | Run `update-agent.test.ts` |
| SC-004 | Changeset present for both spec-kit packages | 1 changeset covering both | Inspect `.changeset/` |

## Assumptions

- The two `plan.md` copies are intended to stay byte-identical (issue asserts this).
- `specs/<feature>/` is the correct per-feature home for tech-stack notes, per generacy#899.
- The Generacy worker's `PHASE_TO_COMMAND` continues to run the bare `/plan` command,
  so the command markdown is the authoritative instruction surface.

## Out of Scope

- Modifying the `update_agent` MCP tool implementation.
- Modifying `constitution.md`'s convention-sync step.
- Any changes to the Generacy prompt wrapper from generacy#899 (it landed there but the
  worker never uses it; this feature fixes the command file the worker actually runs).
- Migrating or trimming existing bloated `CLAUDE.md` files in downstream repos.

---

*Generated by speckit*
