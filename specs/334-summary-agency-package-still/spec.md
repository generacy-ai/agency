# Feature Specification: Update Tier Enum from Starter/Team to New Pricing Tiers

**Branch**: `334-summary-agency-package-still` | **Date**: 2026-04-05 | **Status**: Draft

## Summary

The agency package still has old tier names (`starter`/`team`) in its platform API schemas and tests. These need to be updated to match the new pricing model (`free`/`basic`/`standard`/`professional`/`enterprise`).

The latency and generacy-cloud repos have already been updated. This is a consistency alignment task to bring the agency package in line with the rest of the ecosystem.

## New Tier Names

| Old | New |
|-----|-----|
| starter | basic |
| team | standard |
| (none) | professional |
| enterprise | enterprise (unchanged) |
| (missing) | free (add to enum) |

## Files to Update

### Source Code

**`packages/agency/src/schemas/platform-api/subscription/generacy-tier.ts`**
- Update `z.enum(['starter', 'team', 'enterprise'])` → `z.enum(['free', 'basic', 'standard', 'professional', 'enterprise'])`
- Update JSDoc comments referencing "Starter" and "Team"
- Update example `tier: 'team'` → `tier: 'standard'`

**`packages/agency/src/schemas/platform-api/organization/organization.ts`**
- Update `z.enum(['starter', 'team', 'enterprise'])` → `z.enum(['free', 'basic', 'standard', 'professional', 'enterprise'])`
- Update example `subscriptionTier: 'team'` → `subscriptionTier: 'standard'`
- Update JSDoc mentioning old tier names

### Tests

**`packages/agency/src/schemas/platform-api/subscription/__tests__/generacy-tier.test.ts`**
- Update test iteration from `['starter', 'team', 'enterprise']` to new tier names
- Update expected validation behavior for new tiers

**`packages/agency/src/schemas/platform-api/organization/__tests__/organization.test.ts`**
- Update test iteration from `['starter', 'team', 'enterprise']` to new tier names
- Update expected validation behavior
- Update `subscriptionTier: 'team'` fixture

**`packages/agency/src/schemas/platform-api/subscription/__tests__/humancy-tier.test.ts`**
- Update expected failure for `'starter'`
- Update `tier: 'starter'` fixture

## User Stories

### US1: Schema Consistency Across Repos

**As a** platform developer integrating with the agency package,
**I want** the tier enums to reflect the current pricing model (free/basic/standard/professional/enterprise),
**So that** there are no mismatches between what agency validates and what other services (latency, generacy-cloud) expect.

**Acceptance Criteria**:
- [ ] Tier enum includes all 5 tiers: `free`, `basic`, `standard`, `professional`, `enterprise`
- [ ] No references to `starter` or `team` as tier values in source or tests
- [ ] All tests pass with new tier names
- [ ] JSDoc examples use new tier names

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | Update `generacy-tier.ts` enum to `['free', 'basic', 'standard', 'professional', 'enterprise']` | P1 | Core schema change |
| FR-002 | Update `organization.ts` tier enum to match | P1 | Depends on FR-001 for consistency |
| FR-003 | Update all JSDoc comments and examples to use new tier names | P1 | Developer experience |
| FR-004 | Update all test files to exercise the new tier names | P1 | Validation coverage |
| FR-005 | Ensure `starter` and `team` are rejected as invalid by the schema | P1 | Negative test cases |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | Old tier references in source | 0 | `grep -r 'starter\|team' --include='*.ts'` in affected paths returns no tier-related matches |
| SC-002 | Test suite pass rate | 100% | `pnpm test` passes all affected test files |
| SC-003 | Build success | Clean build | `pnpm build` completes without errors |

## Assumptions

- The new tier names (`free`, `basic`, `standard`, `professional`, `enterprise`) are finalized and will not change
- No runtime services in other repos depend on the old enum values from this package at this time
- The `enterprise` tier is unchanged and does not need migration

## Out of Scope

- Updating any other repos (latency, generacy-cloud are already done)
- Database migration of stored tier values
- UI or user-facing label changes
- Entitlement logic changes (only enum/schema alignment)

## Context

- See `docs/generacy-business-model-pricing.md` in tetrad-development for the full pricing model
- All integrations available at all tiers; free tier gets `github_integration` only for entitlements

---

*Generated by speckit*
