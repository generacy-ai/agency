# Implementation Plan: F5: Scaffold claude-plugin-agency-spec-kit structure

**Feature**: Create the Claude Code plugin structure for agency-spec-kit slash commands
**Branch**: `144-f5-scaffold-claude-plugin`
**Status**: Complete

## Summary

This task scaffolds a new Claude Code plugin package `claude-plugin-agency-spec-kit` that provides slash commands for specification-driven development. The plugin will contain markdown command definitions that orchestrate the Agency MCP tools (from `@generacy-ai/agency-plugin-spec-kit`).

## Technical Context

- **Platform**: Claude Code plugins (markdown-based slash commands)
- **Language**: Markdown (for command definitions)
- **Dependencies**: Requires `@generacy-ai/agency-plugin-spec-kit` MCP server for tool implementations
- **Reference**: Existing speckit plugin at `/workspaces/claude-plugins/plugins/speckit/`

## Project Structure

```
packages/claude-plugin-agency-spec-kit/
├── .claude-plugin/
│   └── plugin.json           # Plugin metadata and configuration
├── commands/
│   ├── specify.md            # Create new feature specification
│   ├── clarify.md            # Identify and resolve ambiguities
│   ├── plan.md               # Generate implementation plan
│   ├── tasks.md              # Generate task list from plan
│   ├── taskstoissues.md      # Convert tasks to GitHub issues
│   ├── implement.md          # Execute implementation tasks
│   ├── checklist.md          # Generate quality checklist
│   ├── analyze.md            # Run consistency analysis
│   └── constitution.md       # Manage project governance
└── README.md                 # Plugin documentation
```

## Implementation Approach

1. **Create directory structure** - Set up the package directory with `.claude-plugin/` and `commands/` subdirectories
2. **Create plugin.json** - Configure plugin metadata with MCP dependency declaration
3. **Copy command files** - Copy all 9 command markdown files from the source speckit plugin
4. **Create README** - Document the plugin's purpose and relationship to the MCP server

## Constitution Check

No `.specify/memory/constitution.md` found in this repository. Standard implementation patterns apply.

## Key Technical Decisions

1. **Copy commands verbatim** - Per clarification, commands are copied from existing speckit plugin rather than creating placeholders
2. **Include constitution.md** - Per clarification, the constitution command is included
3. **MCP dependency** - Plugin declares dependency on `@generacy-ai/agency-plugin-spec-kit` MCP server

## File Operations Summary

| Action | Path | Description |
|--------|------|-------------|
| CREATE | `packages/claude-plugin-agency-spec-kit/.claude-plugin/plugin.json` | Plugin metadata |
| CREATE | `packages/claude-plugin-agency-spec-kit/README.md` | Plugin documentation |
| COPY | `commands/specify.md` | From source speckit plugin |
| COPY | `commands/clarify.md` | From source speckit plugin |
| COPY | `commands/plan.md` | From source speckit plugin |
| COPY | `commands/tasks.md` | From source speckit plugin |
| COPY | `commands/taskstoissues.md` | From source speckit plugin |
| COPY | `commands/implement.md` | From source speckit plugin |
| COPY | `commands/checklist.md` | From source speckit plugin |
| COPY | `commands/analyze.md` | From source speckit plugin |
| COPY | `commands/constitution.md` | From source speckit plugin |

## Risks and Mitigations

| Risk | Mitigation |
|------|------------|
| Source commands may reference unavailable MCP tools | Verify tool names match agency-plugin-spec-kit exports |
| Path references may be incorrect | Update any hardcoded paths during copy |

---

*Generated by speckit*
