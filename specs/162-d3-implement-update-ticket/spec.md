# Feature Specification: D3: Implement update_ticket tool

**Branch**: `162-d3-implement-update-ticket` | **Date**: 2026-01-31 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent D** - Ticket & Tasks Tools (`src/tools/get-ticket.ts`, `create-ticket.ts`, `tasks-to-issues.ts`)

## Description
Implement the `spec_kit.update_ticket` MCP tool that updates existing tickets in the configured backlog provider.

## Acceptance Criteria
- [ ] Create `src/tools/update-ticket.ts`
- [ ] Support updating title, body, state, and labels
- [ ] Use configured BacklogProvider
- [ ] Return updated Ticket
- [ ] Handle not found errors gracefully
- [ ] Support partial updates (only specified fields)

## Tool Definition

```typescript
export function createUpdateTicketTool(
  config: SpecKitConfig,
  getProvider: () => BacklogProvider,
): AgencyTool {
  return {
    name: 'spec_kit.update_ticket',
    description: 'Update an existing ticket in the configured backlog system',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding'],
    inputSchema: {
      type: 'object',
      properties: {
        ref: {
          type: 'string',
          description: 'Ticket reference - URL or identifier',
        },
        title: {
          type: 'string',
          description: 'New ticket title (optional)',
        },
        body: {
          type: 'string',
          description: 'New ticket body (optional)',
        },
        state: {
          type: 'string',
          enum: ['open', 'closed'],
          description: 'New ticket state (optional)',
        },
        add_labels: {
          type: 'array',
          items: { type: 'string' },
          description: 'Labels to add',
        },
        remove_labels: {
          type: 'array',
          items: { type: 'string' },
          description: 'Labels to remove',
        },
      },
      required: ['ref'],
    },
    async execute(params) {
      // Implementation
    },
  };
}
```

## Dependencies
- A1 (provider registry)
- A5 (auto-detect logic)
- F2 (Ticket types)
- F3 (BacklogProvider interface)
- D1 (shared patterns)

## Files to Create/Modify
- `src/tools/update-ticket.ts`
- `src/tools/index.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
