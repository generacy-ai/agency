# Data Model: A5: Auto-detect logic for ticket refs

**Feature**: Ticket reference auto-detection
**Date**: 2026-02-01

## Core Entities

### TicketRef (Existing)

The output type returned by the detection function.

```typescript
interface TicketRef {
  /** Provider identifier (github, jira, shortcut, local, etc.) */
  provider: TicketProvider;

  /** Ticket ID ("123" or "PROJ-123") */
  id: string;

  /** Full URL if available (from URL input or constructed) */
  url?: string;

  /** Original input string */
  raw: string;
}
```

### TicketProvider (Existing)

```typescript
// Extensible string type
type TicketProvider = string;

// Known providers with first-class support
type KnownTicketProvider = 'github' | 'jira' | 'shortcut' | 'linear' | 'local';
```

### BacklogProviderName (Existing)

```typescript
// Implemented providers
type BacklogProviderName = 'github' | 'jira' | 'shortcut' | 'local';
```

## Detection Input/Output

### Function Signature

```typescript
function detectTicketRef(
  input: string,
  defaultProvider: BacklogProviderName
): TicketRef | null;
```

### Input Formats

| Format | Pattern | Provider | Example |
|--------|---------|----------|---------|
| GitHub URL | `github.com/.../issues/N` | github | `https://github.com/owner/repo/issues/123` |
| GitHub PR URL | `github.com/.../pull/N` | github | `https://github.com/owner/repo/pull/456` |
| GitHub Shorthand | `#N` or `owner/repo#N` | github | `#123`, `owner/repo#123` |
| Jira URL | `*.atlassian.net/browse/KEY-N` | jira | `https://co.atlassian.net/browse/PROJ-123` |
| Jira Key | `PROJECT-N` | jira | `PROJ-123`, `ABC-1` |
| Shortcut URL | `app.shortcut.com/.../story/N` | shortcut | `https://app.shortcut.com/ws/story/789` |
| Shortcut Key | `sc-N` | shortcut | `sc-123`, `SC-456` |
| Local Key | `LOCAL-N` | local | `LOCAL-001`, `local-123` |
| Bare Number | `N` | (default) | `123` |

### Output Examples

```typescript
// GitHub URL
{
  provider: 'github',
  id: '123',
  url: 'https://github.com/owner/repo/issues/123',
  raw: 'https://github.com/owner/repo/issues/123'
}

// Jira shorthand
{
  provider: 'jira',
  id: 'PROJ-456',
  raw: 'PROJ-456'
}

// Local shorthand
{
  provider: 'local',
  id: '001',
  raw: 'LOCAL-001'
}

// Invalid input
null
```

## Validation Rules

### Input Validation

| Rule | Behavior |
|------|----------|
| Null/undefined input | Returns `null` |
| Empty string | Returns `null` |
| Whitespace-only | Returns `null` |
| Leading/trailing whitespace | Trimmed before parsing |
| Unrecognized format | Returns `null` |

### Pattern Validation

| Pattern | Requirements |
|---------|--------------|
| Jira key | Uppercase project prefix, dash, numeric ID |
| Shortcut key | `sc-` prefix (case-insensitive), numeric ID |
| Local key | `LOCAL-` prefix (case-insensitive), numeric ID |
| GitHub shorthand | Optional `#`, numeric ID |
| Bare number | Digits only |

## Entity Relationships

```
User Input
    │
    ▼
detectTicketRef(input, defaultProvider)
    │
    ▼
TicketRef ──► BacklogProvider.getTicket(ref.raw)
    │                  │
    │                  ▼
    │            Ticket (full data)
    │
    ▼
Used by tools: get-ticket, create-ticket, update-ticket
```

---

*Generated by speckit*
