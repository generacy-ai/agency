Generated by /plan — do not hand-edit.

# Tech stack: 519-symptom-when-gate-answer

- **Artifact type**: Markdown command playbook (`packages/claude-plugin-cockpit/commands/auto.md`) — no runtime code changes
- **Language**: TypeScript (ESM) for pin tests only
- **Test framework**: Vitest (`pnpm test` in `packages/claude-plugin-cockpit`, `vitest run`)
- **Test file**: `packages/claude-plugin-cockpit/tests/playbook-verification.test.ts` (drift-audit pin suite; helpers `extractSubheadingBlock` et al.)
- **Monorepo**: pnpm workspaces; changesets (baseBranch `develop`) — patch bump `@generacy-ai/claude-plugin-cockpit`
- **Key technical decisions**:
  - Guard lives only inside D.12 step 1's no-record branch (adoption path preserved, Q1)
  - Shape-based (non-positional) `gateKey` runId-segment detection; issue ref = prefix before first `:` (Q2)
  - Two pinned verbatim ledger vocabularies — foreign-run (production wording) and out-of-scope; runId-mismatch evaluated first (Q3)
  - Four-column no-op row `<gateKey-issue-ref> · — · gate-answer · <outcome> · source: ui-gate` (Q4)
  - Strict one-row-per-delivery, no dedup (Q5); no new loop state
  - No MCP tool / wire-schema changes; doorbell source fix is out of scope (generacy#1228)
