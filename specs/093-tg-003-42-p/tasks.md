# Tasks: [#42] [P] Type Definitions

**Input**: Design documents from `/specs/093-tg-003-42-p/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, quickstart.md
**Status**: Complete

## Format: `[ID] [P?] [Story] Description`
- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to

## Phase 1: Setup & Structure

- [ ] T001 Create types directory structure at `packages/agency-extension/src/types/`
- [ ] T002 [P] Verify TypeScript configuration (tsconfig.json) has strict mode enabled

## Phase 2: Core Type Files

- [ ] T003 [P] Define plugin types in `packages/agency-extension/src/types/plugin.ts`
  - Branded type `PluginId`
  - Interface `PluginConfig` with id, enabled, settings
  - Interface `PluginManifest` with metadata and tools
  - Discriminated union `PluginState` (loading | ready | error)
  - Add JSDoc comments for all exported types

- [ ] T004 [P] Define tool types in `packages/agency-extension/src/types/tool.ts`
  - Branded type `ToolId`
  - Interface `ToolInfo` with metadata
  - Interface `ToolExecutionRequest` with parameters
  - Interface `ToolExecutionContext` with workspace info
  - Discriminated union `ToolResult` (success | error)
  - Interface `ToolError` with code, message, details
  - Interface `JsonSchema` for validation
  - Add JSDoc comments for all exported types

- [ ] T005 [P] Define activity types in `packages/agency-extension/src/types/activity.ts`
  - Branded type `ActivityEventId`
  - Interface `ToolCallEvent` with tool invocation details
  - Interface `ActivityFilter` with filtering criteria
  - Interface `ActivityStats` with aggregated metrics
  - Add JSDoc comments for all exported types

- [ ] T006 [P] Define container types in `packages/agency-extension/src/types/container.ts`
  - Branded type `ContainerId`
  - Interface `ContainerInfo` with metadata
  - Discriminated union `ContainerStatus` (running | stopped | starting | error)
  - Interface `ContainerActionResult` with success flag
  - Add JSDoc comments for all exported types

- [ ] T007 [P] Define mode types in `packages/agency-extension/src/types/mode.ts`
  - Branded type `ModeId`
  - Interface `ModeInfo` with name, description, enabled tools, parent
  - Interface `ModeConfig` with id, enabled, tool overrides
  - Interface `ModeTreeNode` with mode, children, isActive
  - Add JSDoc comments for all exported types

## Phase 3: Central Exports & Utilities

- [ ] T008 Create central type exports in `packages/agency-extension/src/types/index.ts`
  - Export all types from plugin.ts
  - Export all types from tool.ts
  - Export all types from activity.ts
  - Export all types from container.ts
  - Export all types from mode.ts
  - Organize exports by category with comments
  - Add usage examples in JSDoc header

- [ ] T009 Add type guard functions in respective type files
  - `isPluginReady()` in plugin.ts
  - `isToolSuccess()` in tool.ts
  - `isContainerRunning()` in container.ts
  - Export type guards from index.ts

## Phase 4: Validation

- [ ] T010 Run TypeScript compiler to verify all types compile without errors
- [ ] T011 Verify no circular dependencies between type files
- [ ] T012 Verify all exports are accessible from index.ts
- [ ] T013 Check JSDoc comments render correctly in IDE tooltips

## Dependencies & Execution Order

**Phase Dependencies**:
- Phase 1 (Setup) must complete before Phase 2
- Phase 2 tasks (T003-T007) can all run in parallel - they define types in separate files with no dependencies
- Phase 3 (T008-T009) requires Phase 2 completion
- Phase 4 (Validation) requires all previous phases

**Parallel Opportunities**:
- T002 can run in parallel with T001 (different concerns)
- T003, T004, T005, T006, T007 can all run in parallel (separate files, no cross-dependencies)
- T010, T011, T012, T013 can run in parallel (different validation checks)

**Critical Path**:
1. T001 (structure) → T003-T007 (type definitions) → T008-T009 (exports) → T010-T013 (validation)

**File Relationships**:
- No type files depend on each other initially
- index.ts depends on all other type files
- Type guards are co-located with their types but exported separately

---

*Generated by speckit*
