# Feature Specification: ## Summary

The orchestrator's merge-conflict handler escalates unresolvable conflicts by applying `blocked:stuck-merge-conflicts` (leaving `waiting-for:merge-conflicts` in place)

**Branch**: `421-summary-orchestrator-s-merge` | **Date**: 2026-07-15 | **Status**: Draft

## Summary

## Summary

The orchestrator's merge-conflict handler escalates unresolvable conflicts by applying `blocked:stuck-merge-conflicts` (leaving `waiting-for:merge-conflicts` in place). `auto.md`'s dispatch table handles `waiting-for:merge-conflicts` (D.11 escalation gate) but has no row for `blocked:stuck-merge-conflicts`, so each occurrence fell through to the generic unrecognized-state escalation. On the snappoll run this happened 3 times (#3, #13, +1), each interrupting the operator with a "never guess" gate whose options don't include the one action that helps (resolve conflicts → advance the `merge-conflicts` gate).

## Evidence

- snappoll#3: `blocked:stuck-merge-conflicts` 2026-07-14T22:41:44Z → operator resolved via D.11-style path by hand (`manual-advance gate=merge-conflicts` comment at 22:43:39).
- snappoll#13: same pattern 2026-07-15 (~15:01 advance + `cockpit_resume`).
- Run summary: "Escalations: 3 unrecognized-state (`blocked:stuck-merge-conflicts`)".
- Shipped `auto.md` (grep): `blocked:` appears nowhere; D.11 triggers only on the verbatim string `waiting-for:merge-conflicts`.

## Suggested fix

In `auto.md`:

1. Extend D.11's trigger to the pair: `waiting-for:merge-conflicts` **or** `blocked:stuck-merge-conflicts` (they co-occur; the blocked label means "auto-remedy already failed", which is exactly D.11's operator-resolution scenario). Same gate, same options (`I've resolved it → cockpit_advance(gate="merge-conflicts")` / Skip / Stop), with the presentation noting the auto-remedy already failed.
2. Ledger line should carry the real source label so the audit trail distinguishes remedy-failed from plain conflict.

Companion (filed on generacy from the same run): the `@generacy-ai/cockpit` classifier has no `blocked:*` tier, so these arrive as `unknown` state — D-class routing needs the classifier fix to see the label at all.

Found during the snappoll dogfood run (cluster stable, orchestrator 0.8.0).


## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
