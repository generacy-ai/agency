# Implementation Plan: Container Detail Webview

**Feature**: Container Detail Webview for Agency VS Code Extension
**Branch**: `059-tg-020-container-detail`
**Status**: In Progress

## Summary

Implement a webview panel that displays detailed container information and real-time log streaming with filtering capabilities.

## Technical Context

| Technology | Version | Purpose |
|------------|---------|---------|
| TypeScript | 5.x | Primary language |
| VS Code Extension API | 1.85+ | Webview framework |
| execa | 8.x | Docker exec for logs |

## Project Structure

```
packages/agency-extension/src/
├── views/
│   ├── containers/
│   │   ├── ContainerDetailPanel.ts    # NEW: Webview panel class
│   │   └── container-detail.html      # NEW: HTML template
│   └── webview-base.ts                # Extends existing base class
└── __tests__/
    └── views/
        └── ContainerDetailPanel.test.ts # NEW: Tests
```

## Key Interfaces

```typescript
interface ContainerDetailMessage {
  type: 'start' | 'stop' | 'rebuild' | 'filterLogs';
  data?: { filter?: string };
}
```

## Dependencies

- Extends `WebviewBase` from `src/views/webview-base.ts`
- Uses `ContainerService` from `src/services/ContainerService.ts`

---

*Generated by speckit*
