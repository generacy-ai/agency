# Feature Specification: C2: Implement check_prereqs tool

**Branch**: `156-c2-implement-check-prereqs` | **Date**: 2026-01-30 | **Status**: Draft

## Summary

## Parent Epic
Part of #139

## Agent Assignment
**Agent C** - Path & Template Tools (`src/tools/get-paths.ts`, `check-prereqs.ts`, `copy-template.ts`, `update-agent.ts`)

## Description
Implement the `spec_kit.check_prereqs` MCP tool that validates required files exist before operations.

## Acceptance Criteria
- [ ] Create `src/tools/check-prereqs.ts`
- [ ] Support requiring: spec.md, plan.md, tasks.md
- [ ] Return list of available optional documents
- [ ] List available contracts and checklists
- [ ] Return validation status with specific missing files
- [ ] Useful for gating workflow steps

## Tool Definition

```typescript
export function createCheckPrereqsTool(config: SpecKitConfig): AgencyTool {
  return {
    name: 'spec_kit.check_prereqs',
    description: 'Check prerequisites for a command. Validates required files exist and returns list of available optional documents.',
    namespace: 'spec_kit',
    outputPattern: 'terse',
    modes: ['coding', 'research'],
    inputSchema: {
      type: 'object',
      properties: {
        require_spec: {
          type: 'boolean',
          default: true,
          description: 'Whether spec.md is required (default: true)',
        },
        require_plan: {
          type: 'boolean',
          default: false,
          description: 'Whether plan.md is required (default: false)',
        },
        require_tasks: {
          type: 'boolean',
          default: false,
          description: 'Whether tasks.md is required (default: false)',
        },
        include_tasks: {
          type: 'boolean',
          default: false,
          description: 'Include tasks.md in available_docs if it exists',
        },
        branch: {
          type: 'string',
          description: 'Optional branch/feature name. If not provided, uses current.',
        },
        cwd: {
          type: 'string',
          description: 'Working directory (defaults to process.cwd())',
        },
      },
    },
    async execute(params) {
      // Implementation
    },
  };
}
```

## Return Structure

```typescript
interface PrereqsResult {
  valid: boolean;
  missing: string[];           // List of missing required files
  available_docs: string[];    // List of available optional documents
  contracts: string[];         // List of contract files in contracts/
  checklists: string[];        // List of checklist files in checklists/
  feature_dir: string;         // Resolved feature directory path
}
```

## Dependencies
- C1 (get_paths for path resolution)
- B1 (fs utilities)
- F2 (types)

## Files to Create/Modify
- `src/tools/check-prereqs.ts`
- `src/tools/index.ts`

## References
- Current implementation: `/workspaces/claude-plugins/plugins/speckit/mcp-server/src/tools/prereqs.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
