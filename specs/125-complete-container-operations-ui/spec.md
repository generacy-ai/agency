# Feature Specification: Complete Container Operations UI in VS Code extension

**Branch**: `125-complete-container-operations-ui` | **Date**: 2026-01-23 | **Status**: Draft

## Summary

## Overview

The container management commands in the Agency VS Code extension have stub implementations. The ContainerService and ContainerTreeProvider exist but several operations are not fully implemented.

## Current State

These commands are registered but stubbed:
- `agency.startContainer` - Start a dev container
- `agency.stopContainer` - Stop a running container
- `agency.rebuildContainer` - Rebuild container from Dockerfile
- `agency.viewContainerLogs` - View container logs

The `ContainerTreeProvider` and `ContainerDetailPanel` exist but may not be fully functional.

## Requirements

### Start Container
- Detect devcontainer.json in workspace
- Start container using Docker CLI or Dev Containers extension API
- Show progress notification
- Update tree view when started

### Stop Container
- Stop running container gracefully
- Update tree view
- Handle MCP connection cleanup

### Rebuild Container
- Rebuild from current devcontainer.json
- Show build progress
- Restart MCP connection after rebuild

### View Logs
- Open output channel with container logs
- Stream logs in real-time
- Support log filtering

### Container Tree View
- Show container status (running/stopped/building)
- Show container details (image, ports, volumes)
- Context menu for operations

## Technical Notes

- May need to integrate with VS Code Remote - Containers extension
- Docker CLI can be used as fallback
- MCP connection state needs to sync with container state

## Acceptance Criteria

- [ ] Can start/stop containers from tree view
- [ ] Rebuild works and restarts MCP connection
- [ ] Logs viewable in output panel
- [ ] Status updates reflect actual container state
- [ ] Works with standard devcontainer.json setup

## Related Files

- `packages/agency-extension/src/views/ContainerTreeProvider.ts`
- `packages/agency-extension/src/services/ContainerService.ts`
- `packages/agency-extension/src/webviews/ContainerDetailPanel.ts`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
