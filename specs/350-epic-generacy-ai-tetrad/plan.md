# Implementation Plan: claude-plugin-cockpit scaffold + marketplace entry

**Feature**: Scaffold `claude-plugin-cockpit` package with empty `/cockpit` namespace and register it in `.claude-plugin/marketplace.json`
**Branch**: `350-epic-generacy-ai-tetrad`
**Date**: 2026-06-26
**Spec**: [spec.md](./spec.md)
**Status**: Complete

## Summary

Create a new Claude Code plugin package `packages/claude-plugin-cockpit/` containing only static manifest, README, and an empty `commands/` directory preserved by `.gitkeep`. Register the plugin in the root `.claude-plugin/marketplace.json` as a single new entry alongside the existing `agency-spec-kit` entry. No code, no build step, no MCP coupling — the deliverable is four committed files and one JSON object edit. The structure mirrors the existing `packages/claude-plugin-agency-spec-kit/` shape so downstream Epic Cockpit issues (#351–#360) can drop verb files into `commands/` without further scaffolding work.

## Technical Context

**Language/Version**: None (no compiled code in this scaffold)
**Primary Dependencies**: None
**Storage**: Repository files only
**Testing**: Manual — JSON schema validation + install probe against a Claude Code environment with the generacy marketplace registered via `extraKnownMarketplaces`
**Target Platform**: Claude Code (any OS)
**Project Type**: Monorepo package (pnpm workspaces, but no `package.json` in this issue)
**Performance Goals**: N/A
**Constraints**:
- Plugin loader globs `commands/*.md` — placeholder MUST be `.gitkeep`, never a `.md` file (per clarifications Q3)
- Manifest must omit `commands` and `requires` blocks (FR-005)
- Marketplace entry must reuse the exact `description` from `plugin.json` (FR-001, FR-004)
**Scale/Scope**: 4 new files + 1 JSON edit; ~5 lines net in marketplace.json

## Constitution Check

No `.specify/memory/constitution.md` is present in the repo — no gates apply.

## Project Structure

### Documentation (this feature)

```text
specs/350-epic-generacy-ai-tetrad/
├── spec.md              # Feature specification (existing, read-only)
├── clarifications.md    # Clarification answers (existing)
├── plan.md              # This file
├── research.md          # Technology/pattern decisions
├── data-model.md        # Manifest + marketplace-entry schemas
├── quickstart.md        # Install + verification steps
├── contracts/
│   ├── plugin.schema.md      # Reference to upstream plugin.json schema
│   └── marketplace.schema.md # Reference to upstream marketplace.json schema
└── checklists/          # (empty — not generated by this plan)
```

### Source Code (repository root)

```text
packages/claude-plugin-cockpit/                # NEW package directory
├── .claude-plugin/
│   └── plugin.json                            # FR-001: name, description, author
├── commands/
│   └── .gitkeep                               # FR-002: preserves empty dir, invisible to loader
└── README.md                                  # FR-003: install + planned-verbs table

.claude-plugin/
└── marketplace.json                           # MODIFIED: append `cockpit` entry to `plugins` array
```

**Structure Decision**: Mirror `packages/claude-plugin-agency-spec-kit/` exactly. No `package.json`, no `src/`, no tests directory — this is a static-asset plugin whose runtime artifacts are markdown files in `commands/`.

## Implementation Phases

### Phase 0: Verify reference shape
- Re-read `packages/claude-plugin-agency-spec-kit/.claude-plugin/plugin.json` and `README.md` to confirm copy targets.
- Re-read root `.claude-plugin/marketplace.json` to confirm `plugins` array shape.

### Phase 1: Create the package files
1. Create `packages/claude-plugin-cockpit/.claude-plugin/plugin.json` with `name`, `description`, `author` per FR-001 + clarification Q1.
2. Create `packages/claude-plugin-cockpit/commands/.gitkeep` (empty file) per FR-002 + Q3.
3. Create `packages/claude-plugin-cockpit/README.md` per FR-003 + Q4 + Q5 — install via `extraKnownMarketplaces` using identifier `generacy-ai/agency`; include planned-verbs table with `(coming in #351–#360)` markers.

### Phase 2: Register in marketplace
4. Append a new object to `plugins` in `.claude-plugin/marketplace.json`: `{ name: "cockpit", description: <same as plugin.json>, author: <generacy block>, source: "./packages/claude-plugin-cockpit", category: "development" }`. Do not modify the existing `agency-spec-kit` entry.

### Phase 3: Validate
5. Validate `marketplace.json` against `https://anthropic.com/claude-code/marketplace.schema.json` (via `$schema` reference already present in the file).
6. Validate `plugin.json` parses as JSON; confirm no `commands` or `requires` keys (FR-005).
7. Confirm directory layout matches `claude-plugin-agency-spec-kit` (FR-006 / SC-004).

## Complexity Tracking

No constitution violations; no complexity entries.

## Open Risks

| Risk | Mitigation |
|------|------------|
| Plugin loader rejects a namespace with zero commands | If install fails, fall back to a no-op stub command (e.g., `commands/_placeholder.md` with a comment) — this would deviate from clarification Q3 and should be raised back to the user before changing the file shape. |
| Marketplace schema rejects an empty-commands plugin | Re-validate after each `marketplace.json` edit; if rejected, treat as a spec gap and surface to the user. |
| Identifier `generacy-ai/agency` is not the canonical extraKnownMarketplaces value at install time | Cross-check with the existing entry in `.claude/settings.json` before publishing README. |
