# Deprecated

These command files have moved to `@generacy-ai/agency-plugin-spec-kit`.

Install commands via:
```bash
npx agency-spec-kit install-commands
```

Or programmatically:
```typescript
import { commandsDir } from '@generacy-ai/agency-plugin-spec-kit/commands';
```
